# ÒsánVault Services

Canonical service boundaries for internal platform capabilities.

Initial boundaries:

- `api/` — canonical application API boundary
- `verify/` — verification capability boundary
- `treasury/` — treasury and ledger capability boundary
- `agents/` — ÒsánVault-scoped AI orchestration boundary

Existing implementations are not replaced until their contracts and behavior are validated.
